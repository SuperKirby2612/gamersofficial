const DiscordjS = require('discord.js')

module.exports = {

    name: 'lock',
    description: "Makes the everyone role not able to type in the current channel (MODS ONLY) Syntax: -g lock",
    async execute(message, args) {
        if (member.roles.cache.has('763422695148748810')); {
            message.channel.send("Testing the member.roles.cache.has function")
        }
    }
}